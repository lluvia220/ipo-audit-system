services:
  - type: web
    name: ipo-audit-backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production